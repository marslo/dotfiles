HOST  *
      LogLevel                  ERROR
      GSSAPIAuthentication      no
      StrictHostKeyChecking     no
      # downward compatibility for RSA
      HostkeyAlgorithms         +ssh-rsa
      PubkeyAcceptedAlgorithms  +ssh-rsa
      IdentitiesOnly            yes
      UserKnownHostsFile        ~/.ssh/known_hosts
      # to resolve keychain issues on cross-platform
      IgnoreUnknown             UseKeychain

Include config.d/*

HOST  *
      IdentityFile              ~/.ssh/ed25519
      IdentityFile              ~/.ssh/rsa
      IdentityFile              ~/.ssh/ed25519_gpg

# vim:tabstop=2:softtabstop=2:shiftwidth=2:expandtab:filetype=sshconfig:
